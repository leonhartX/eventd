<%= form_tag shares_path, method: :post do |f| %>
  <%= text_area_tag :message, "Join #{@event.title}: #{request.original_url}" %>
  <%= submit_tag "Share to #{current_user.provider}", class: "btn btn-primary" %>
<% end %>